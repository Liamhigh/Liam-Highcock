# Git attributes for Android project
# Ensure consistent line endings across platforms

# Auto detect text files and perform LF normalization
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.java text
*.kt text
*.xml text
*.gradle text
*.properties text
*.kts text
*.md text
*.txt text
*.json text
*.yml text
*.yaml text

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.mov binary
*.mp4 binary
*.mp3 binary
*.pdf binary
*.zip binary
*.jar binary
*.apk binary
*.ap_ binary
*.dex binary
*.class binary
*.so binary

# Gradle wrapper should always use LF for cross-platform compatibility
gradlew text eol=lf
*.sh text eol=lf

# Windows batch files should use CRLF
*.bat text eol=crlf
